Basic:
-> props is the first parameter of a functional component
-> import AlexaImage from "./images/alexa.png"
-> if image size < 10,000 bytes or 9.7Kb then, image raw data is taken and converted into base64 encoded string and gets inlined
-> otherwise, it is treated as a separate file

Event:
-> detect a user doing something inside of our app
-> event handler - function that executes when an event is emitted
-> anytime we wire up an event handler on a plain jsx element, an event object is always passed in

State:
-> data that changes as the user interacts with our application
-> update content on screen (re-render) automatically
-> only way to change what content react shows
-> libraries that appear to update content, use the state system behind the scene
-> when a component re-renders, it's children re-render as well
-> a single piece of data can server as both props and state
-> every time a component is rendered, a brand new set of variables are declared

map:
-> takes every element from an array and passes it to a transformation function and add into a brand new array

HTTP:
-> think of request-response as text message in a special format
-> request: 
  - (i) request line: GET https://api/unsplash.com/images/search HTTP1.1
  - (ii) headers: Accept-Version: v1, Authorization: Client-ID ABC123
  - (iii) body
-> reponse:
  - (i) status line: HTTP/1.1 200 OK
  - (ii) headers: Content-Length: 1000, Content-Type: application/json
  - (iii) body: [{id:"123"}]
-> headers customize the request in some way or provide additional information to the server
-> method indicates the general purpose of the request


Data Flow:
-> to share info between sibling components, we always have to involve parent component
-> to communicate from child up to parent, we need to treat it like an event and pass an event handler down


Form:
-> if you place an input inside a form and press enter key, some automatic behavior of the browser will kick in
-> browser will cause the form element to trigger a special event called 'submit'
-> browser will collect all the information inside the form and make a network request with them
-> this would work even if the user was not making use of javascript at all
-> why do we use onChange and value property on input element?
-> to steal control of the value of the input from browser and give it to state system

Updating a list:
-> state change - swap first two records
-> need to update html
-> option 1: remove all existing html tied to this list and recreate them. computationally very expensive
-> option 2: during initial render, apply a unique key to each element while mapping step. after re-rendering, compare the keys 
             on each element to the keys from previous render to figure out what changes were made to apply minimal set of
  	     changes to the existing dom elements.

Controlled Input:
-> whenever we control any type of form controls (input, radio, textarea) where we provide both value and onChange props


Where to define state?
-> find all the components that need to use this state
-> define the state in the lowest common parent

How to update arrays or objects inside react applications?
-> when we create a state, react holds a reference to that current version state
-> react compares the memory address of arrays and objects that was created as state variables
-> do not mutate/modify the existing array/object, if so react will not re-render
-> we need to give react a brand new array or object
-> otherwise, a bug would appear if we add in optimization that says only re-render this component if the
   incoming props are different than the last time

How to add, remove Element to an array:
-> const updatedColors = [...colors.slice(0, index), newColor, ...colors.slice(index)]
-> const updatedColors = colors.filter((color, index) => color === "green) / colors.filter((color, index) => index !== 5)

How to modify element in an array:
-> In JS, whenever we create an object, all keys must be unique. if we have duplicate keys, the earlier key will be overwritten
-> const updateBookById = (id, newTitle) => {
     const updatedBooks = books.map((book) => {
       if(book.id === id){
          return {...books, newTitle }
       }
       else{
          return book
       }
    }
   setBooks(updatedBooks)

  }
-> const changeColor = (color) => {
      const updatedFruit = {
           ...fruits,
           color: color 
        }
     setFruit(updatedFruit)
  }
-> const removeColor = () => {
      const {color, ...rest} = fruit
      setFruit(rest)
   }


Tricky things around useEffect:
-> understanding when arrow function gets called
-> understanding the arrow function's return value
-> understanding stale variable references

useEffect:
-> function passed into useEffect always gets called after initial render of after the DOM is updated
-> on the following renders, that function might be called depending on the dependency array
-> if we don't put a dependency array, the function will be called after every render
-> if there are some variables inside dependency array, the function will be called whenever that value of variables changes
-> we cannot use async await inside the function we pass into
-> the function we pass into useEffect, we are only allowed to return another function, known as cleanup 
-> react will hold onto the cleanup function for you and call automatically at some point of time in future
-> it gets called immediately after second render, just before running the useEffect again
-> then react creates a brand new cleanup function
-> it keeps going on like this
-> the cleanup function is only called if your arrow function is about to be called again
-> undo something before our next useEffect runs
-> common when we are watching for events manually on certain elements that are being rendered inside the DOM
-> commonly used with modal window or dropdown

Context:
-> a communication channel but redux is all about organization of data
-> data can be shared across all components
-> a component can reach out to context and ask for a specific piece of data
-> create context - returns a context object
-> it has two properties: provider and consumer. both are react components
-> provider: component used to specify what data we want to share
-> consumer: component used to get access to data
-> <BookContext.Provider value={}> <MyComponent /> </BookContext.Provider>
-> value prop contains data that will be shared
-> the components that need to have access this value, we have to put them in between the provider
-> the custom provider shows the built-in provider
-> <Provider>App</Provider>
-> the App component will show up as a prop to the provider called children

Application State:
-> data that is used by many different components
-> often a good idea to put into context
Component State:
-> data that is used by very few components


Hooks:
-> functions that add additional features to our component
-> useState: allow a component to use state system
-> useEffect: allow a component to run code at specific points in time
-> useContext: allow a component to values stored in context
-> useCallback: 
    - tell react your function isn't actually changing over time
    - fixes bug around useEffect and other similar situations
    - follows similar conventions as useEffect
    - works differently in first render and subsequent re-renders
    - it does not even run your function
    - at initial render, useCallback does nothing, it returns the function you put in as first argument
    - if second argument is empty array, it gives you back the original function from first render
    - if second argument has elements that have changed since last render, it will give you the new version of that function

Custom Hooks:
-> functions we write to make reusable bits of logic
-> function that contains some reusable logic

When to create a custom hook?
-> find code in a component that is related to one or two piece of state
-> do not include any jsx
-> copy paste it all into a helper function in a separate file
-> fix all the broken references in both files

Stale Variable Reference (out-of-date):
-> possible bug anytime your useEffect contains or creates a function that refers to a variable


Children Prop:
-> if you create a custom component and place anything inside the pair of tags, that will be taken automatically 
   and provided as a prop called children


Events and State Design Process:
(I) what states and event handlers are there?
  - list out what a user will do and changes they will see while using your app
  - categorize each step as state or event handler
  - group common steps, remove duplicates, rewrite descriptions
(II) what name and type?
  - look at mockup, remove or simplify parts that aren't changing
  - replace remaining elements with text descriptions
  - repeat #4 and #5 with a different variation
  - imagine you have to write a function that returns the text of steps #5 and #6. in addition to your component props,
    what other arguments would you need?
(III) where is it defined?
  - decide where each event handler and state will be defined
  - does any component besides accordion reasonably need to know which item is expanded?
  - if other component needs to know that state, define in the lowest common parent
  - otherwise, define in the component only where it is needed
  - event handler should be usually defined in the same component as the state it modifies


Conditional Rendering:
-> react does not print booleans, nulls, undefined
-> In JS, || gives back the first truthy value
-> In JS, && gives back the first falsey value or the last truthy value 

Inline Event Handlers:
-> for every iteration of the mapping function
-> we create a brand new arrow function and pass it off to events such as onClick
-> usually event handlers receive an event object as the first argument
-> but if we call event handler manually and pass some argument, those arguments will be received


Delayed State Update:
-> react: oh you want to update state...yeah I'll get around to it in the future
-> react does not process state updates instantly
-> the delay is known as batching
-> react allowing us to make several updates and it will process all those updates at the same time to improve performance
-> solutions:
  - (I) get react to process state updates instantly (not ideal, app gets slower)
  - (II) get access to the most up-to-date value of state (functional update)
  - use functional update whenever our new update depends on the old value of state (conditional update)

Existence Check Helper:
-> colors = undefined;
-> colors.length - cannot read properties of undefined
-> colors?.length - undefined


How to implement click event handler outside the dropdown area so that it closes the dropdown?
(I) In plain JS:
  - document-wide click handlers
  - event capturing/bubbling
      - when an event occurs, browser wants to find event handlers to call
      - order in which search occurs is divided into three phases: capture, target, bibble
  - checking element inclusion
(II) In react:
  - useEffect
  - useRef

Capture Phase:
-> go to the far most parent of the clicked element, see if it has handlers. go to the second far most parent until the element 
   that was clicked/interated with (excluding that element)
-> if it finds the event handler, call it
-> frequently disabled

Target Phase:
-> go to the clicked/interacted element, check to see if it has event handlers
-> if so, it will be called

Bubble Phase:
-> go to the immediate parent and see if it has event handlers
-> then go the parent of the parent and see if it has event handlers

-> document.addEventListener('click', handleClick)
-> if we set event handlers with two arguments, then we will only for event handlers in target phase and bubble phase
-> document.addEventListener('click', handleClick, true)
-> it will look for event hanlders in capture phase and target phase
-> 90% of the time, we want it false
-> false by default


Why do we need Capture phase to implement Dropdown?
-> react does not re-render instantly, there is some gap of time
-> but it takes even longer for the entire bubble phase to be processed
-> by enabling capture phase, the custom event handler will be executed before react re-renders
-> because capture phase starts looking for event handlers from the furthest parent element
-> body - div [custom click event handler running here] [capture phase] 
-> option div [react click event handler running here] [target phase] 
-> div - body [bubble phase] 


Function to get current time:
-> performance.now()
-> number of milliseconds elapsed since you loaded this page

Issues with element references:
-> document.querySelector(".w-48")
-> it returns the first element of this class name  
-> will not work if we show multiple different dropdowns
-> separate event listener for each dropdown
-> the root element of each div has a class name of w-48
-> the click occurred inside of which dropdown
-> we need to make sure every dropdown has a reference to the html it created

useRef:
-> allows a component to get a reference to a DOM it creates
-> 95% of the time used with DOM elements but can hold a reference to any value
-> it gives us an object with current property that holds the reference
-> const divElement = useRef()
-> <div ref={divElement}> </div>
-> divElement might be null if we do not assign it to any html element


Standard Browser Behavior:
-> when browser loads a new html document, all existing JS variables and code is dumped


How Navigation Works in React:
-> User types our address in
  - always send the index.html file
  - when app loads up, look at the address bar and use it to decide what content to show
-> User clicks a link or presses the back or forward button:
  - stop the browser's default page switching behavior
  - figure out where the user was trying to go
  - update content on the screen and address bar to trick the user into thinking they swapped pages
 

Initial Render:
-> send index.html regardless of what address they are trying to access
-> html file will have a script tag inside that tells the browser to make a second request and get the bundle.js file
-> once the browser gets that file, the react application will be executed and start up
-> the react app will look at the address bar as soon as possible
-> it will look at the route portion of the address and apply a series of routing rules 
-> display the appropriate component according to the routes

User is already at our page and clicks some link/button:
-> intercept the navigation event and prevent browser from doing typical navigation process
-> handle navigation ourselves using JS code inside of the react app
-> take a look at where the user was trying to go to
-> update the address bar so that they think they navigated
-> apply routing rules once again - display the appropriate component according to the routes

Benefits of navigation in this way:
-> we are not resetting our javascript environment
-> when a user goes back to component they saw previously, we can show data that we already fetched instantly 
   without making another request to outside api
-> only works if the state is not defined inside the component because by default when a component is removed
   from the screen, all it's states gets dumped
-> if we store inside of parent component or a context object, it would be persisted

URL:
-> doman name:port/path
-> localhost:3000/user
-> we only care about the path
-> window.location.pathname

Changing the Address Bar:
-> window.location = "http://localhost:3000/dropdown"
-> causes a full refresh, make a full request, find html, load it up
-> window.history.pushState({}, '', '/dropdown')
-> updates the address bar but does not cause a refresh

Handling back/forward button:
-> window emits a 'popstate' event if the current url was added by pushState method instead of manual typing
-> clicking on back/forward button will not cause a refresh if the current address I'm at was added with pushState
-> browser keeps track of history using stack
-> to detect a user going back/forward we will use popstate event
-> the only reason we have the state currentPath, setCurrentPath is to cause a re-render when user clicks back/forward button


Programmatic Navigation:
-> trigger navigation from code in response to something that is not directly user driven
-> banking software - logout after 5 minutes of inactivity
-> the navigate function will call pushState to update the path inside address bar
-> it will also update currentPath state


Position Absolute:
-> puts the element at the top left corner of the closest parent with a position other than static  
-> if no static parent, it will be positioned at the top left corner of the html document

inset-0:
-> it means top:0 left:0 bottom:0 right:0
-> if the element is absolute, it will expand to fill the height and width of closest parent with a non-static position
-> if no static parent, it will expand to fill the entire html document


Portal:
-> changes the way react works
-> tells react don't place the Modal's html here as a child of the html for ModalPage component
-> instead, we want to take all the html produced by Modal component and place at some other location inside html document
-> it gurantees our modal component will never have a positioned parent because it's parent is the body
-> so it will fill up the entire screen


Table Requirements:
-> variable number of rows and columns
-> number of columns does not have to match with number of properties inside the object
-> some columns are sortable which can sort different kinds of values
-> cells can be calculated using multiple properties
-> cells can display arbitrary data
-> to make table dynamic, we will pass a second object 'config' that will tell the component how we want to render our table
-> number of objects inside config will be number of columns inside the table
-> render function will describe how we will produce the content that is displayed inside each column


Sorting in JavaScript:
-> data.sort()
-> converts elements into string and then sorts
-> to fix these, we need to add comparator function
-> data.sort((a, b) => a - b);  // only works for numbers
-> data.sort((a, b) => a.localeCompare(b))  // only works for strings


Fragments:
-> very simple component that does not have any semantic meaning, it only displays the children

Input Element with Type Number:
-> even though we type number, event.target.value is a string
-> to solve it, use parseInt or parseFloat()
-> if we empty the input box, it will result in NaN, because parseInt("") gives NaN
-> to solve it, const value = parseInt(event.target.value) || 0

useReducer:
-> hook for managing state
-> defines state in a component like useState
-> changing this state makes component re-render
-> useful when you have several different closely related states
-> and future values of future state depend on the current state
-> use either useState or useReducer inside a single component
-> const [state, dispatch] = useReducer(reducer, {count: 0, valueToAdd: 0})
-> by convention, we take all states that are required to make our component work correctly 
   and combine them together into one single object as opposed to separate variables


How useReducer works?
-> to update state we call dispatch()
-> when we call dispatch, react will find reducer function and run it
-> const reducer = (state, action) => {}
-> the first argument state is whatever the current state of our component is
-> the value of second argument action is whatever we pass into dispatch (only one argument can be passed)
-> the reducer function will run and whatever we return from it, will be our new state


Rules around reducer function:
-> whatever you return will be your new state
-> if you return nothing your state will be undefined
-> no async/await, no requests, no promises, no outside variables
-> do not modify the state object in any way
-> when reducer function runs, the only information it has access to are the arguments (state, action)
-> we have only one reducer function


How to tell dispatch which state to update?
-> when we call dispatch, we need to pass along some info to tell the reducer how the state should be updated
-> we will always pass an action object
-> it will have a 'type' property that is a string. it will tell reducer which state to update
-> if we need to communicate some data, it will placed in the 'payload' property of the action object

Some Design Considerations:
-> try to put most of the logic inside of reducer function and keep the dispatch cleam
-> one of the goals of reducers is to have a very specific set of ways that state can be changed


Immer:
-> a library that lets you write code to directly mutate state
-> you do not have to return state 
-> still return in each case, otherwise you get fallthrough


Redux:
-> a library for managing state using the same techniques as useReducer
-> we create a separate 'store' to create and maintain our states
-> components can connect to the store and access state
-> redux library does not assume you are using react
-> react-redux library helps communicate between react and redux sides of your project
-> multiple reducer functions, each managing a different part of a state
-> split up the responsibility of managing state inside of our application


Why are redux and useReducer very popular?
-> the dispatch function pattern
-> whenever we want to change our state in any way, we have to call dispatch function
-> central point of changing any state
-> understanding why state is updating and what component is triggering the update
-> easy to add-in additional debugging features
-> downside: reducer function has to be told why it is being executed 
   and what state updates it needs to make (action.type)


Redux Toolkit:
-> wrapper around plain redux
-> specifically simplifies the action type creation process


Redux Store:
-> store is an object that holds all of our states for our entire application
-> usually the react-redux library will interact with or handle the store for us
-> to manually interact with store, dispatch an action store.dispatch({type: "song/addSong", payload: "new song"})
-> to see what state exists in the store, console.log(store.getState())
-> the keys of the big state object are set when the store is created
-> the values of the keys are produced by individual reducers that are loaded in the store
-> const store = configureStore({reducer: {songs: songsSlice.reducer}})

Redux Slice:
-> slices automatically create reducers and action types
-> slices do three things
-> defines some initial state
-> combines mini-reducers into a big reducer
-> creates a set of 'action-creator' functions
-> slices save from having to write out all this boilerplate (action types, switch statements, cases)
-> when we define a slice, we write out a set of mini-reducer functions on that reducers property
-> you can view each of these functions as individual case statement inside of a larger switch statement
-> the slice has a property called reducer which combines all the individual reducers
-> this combined reducer is loaded into the redux store
-> each mini-reducer function is responsible for a particular state update
-> the mini-reducers use immer library so we can mutate state
-> how does the big reducer know which mini-reducer function to run?
-> the slice will take the name of the slice, add a forward slash and the name of the function
-> const songsSlice = createSlice({name: "song", initialState: []}, reducers: {addSong(state, action){}})
-> song/addSong
-> the state argument inside mini-reducer function is the piece of state managed by this reducer


Action Creators:
-> functions that create action objects with type automatically for us so we don't have to memorize the pattern
-> the createSlice function will take a look at the names of all mini-reducer functions
-> then it will create a new object that contains functions for each reducer function with the same name
-> when called, these functions return an action which we can dispatch immediately
-> saves us from having to write out action objects
-> {addSong(payLoad){return {type: "song/addSong", payload: payload}}}
-> console.log(songsSlice.actions.addSong("new song"));
-> store.dispatch(songsSlice.actions.addSong("new song"))
-> console.log(songsSlice.actions), an object that contains all the action creators


React-Redux:
-> a library that connects react with redux
-> the key part of this library is the Provider component
-> when we create this provider we pass in our redux store to it
-> it uses context system to make the store available to all components inside of our application


state:
-> inside of a slice file, in a reducer function, state means the piece of state it manages
-> everywhere else, state means the whole state object in the store

*** reducers are functions that change the state in some particular way
*** useDispatch uses context system to reach up to the provider at the top of component hierarchy
    and get access to the dispatch function from the redux store

Updating state from a component:
-> when a user does something, call the action creator to get an action object
-> then dispatch it
-> dispatch(addSong(song))

Accessing state in a component:
-> call the useSelector hook, pass in a selector function
-> useSelector((state) => state.songs);


Changing state:
-> add a reducer function to one of your slices to change the state in some way
-> export the action creator that the slice automatically creates
-> from the component, call the action creator to get an action object and then dispatch it